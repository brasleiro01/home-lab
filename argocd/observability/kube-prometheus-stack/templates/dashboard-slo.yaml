# Dashboard de SLOs (Sloth) — error budget + burn rate. Vai pra pasta SRE.
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-slo
  namespace: observability
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: SRE
data:
  slo.json: |
{{ .Files.Get "dashboards/slo.json" | indent 4 }}
